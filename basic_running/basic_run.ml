(* Execution of Basic code *)

#use "basic_parsing/basic_type"

let run (code: basic_code) : unit =
  (* Variables: array of size 29, storing the content of each variable A..Z, r, theta, Ans *)
  let var = Array.make 29 0 in
  ();;